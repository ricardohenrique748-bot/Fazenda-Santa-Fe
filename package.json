{
    "name": "malut-solucoes",
    "version": "1.0.0",
    "description": "Sistema de Gestão Malut Soluções",
    "scripts": {
        "dev": "npm run dev --prefix frontend",
        "backend": "npm run start:dev --prefix backend",
        "install-all": "npm install --prefix frontend && npm install --prefix backend"
    }
}