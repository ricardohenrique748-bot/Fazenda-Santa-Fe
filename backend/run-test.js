const { spawn } = require('child_process'); const p = spawn('npm', ['run', 'start'], { shell: true }); p.stdout.on('data', d => console.log(d.toString())); p.stderr.on('data', d => console.error(d.toString())); setTimeout(async () => { try { const r = await fetch('http://localhost:3000/grupos-equipamento', { method:'POST', headers:{'Content-Type':'application/json'} }); console.log('STATUS:', r.status); console.log(await r.text()); } catch(e) { console.error('FETCH ERR:', e); } process.exit(0); }, 5000);
